scores = [100, 97, 88, 91]
hap, count = 0, 0
for score in scores:
    hap = hap + score
    count = count + 1
print(average)

#scores = [100, 97, 88, 91]
#average =
#print(average)

#import statistics # 통계모듈 (각종 통계함수를 제공)
#scores = [100, 97, 88, 91]
#average = statistics.mean(scores)
#print(average)